# Tasks — Task 3 Promo / Demo Page

- [x] Design a technical visual identity grounded in the relational schema (deep slate background, teal accent, IBM Plex Mono + IBM Plex Sans pairing)
- [x] Build an ER-diagram hero (inline SVG) showing User, Project, ProjectMember, Task and their real relationships
- [x] Build the architecture flow section (Frontend → REST API → Backend → Database)
- [x] List the tech stack as pills (Node.js, Express, Prisma, PostgreSQL/Neon, Vercel)
- [x] Build the verified CRUD checklist section (Create, Read, Update, Delete, health check, config safety)
- [x] Add links to the live API and GitHub repo
- [x] Implement in-browser narration using the Web Speech API, with play/stop controls and a transcript toggle
- [ ] Swap in the final GitHub repo URL if it changes before deployment
- [ ] Deploy `index.html` to GitHub Pages (new repo or a `/docs` folder / `gh-pages` branch on the existing task3-persistent-data-layer repo)
- [ ] Use this page as the intro and outro screen when screen-recording the CRUD demo video
- [ ] Record the middle section of the video: live Create → Read → Update → Delete calls against the deployed API (e.g. via reqbin or Postman), narrated live or with this page's narration as a lead-in
